# Rust
/target

# SQLx offline query cache (uncomment if using `cargo sqlx prepare`)
# .sqlx

# Environment variables
.env
.env.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Local clone of the Keeplin client (used for reference)
keeplin/
# Graphify knowledge graph: generated and published by CI, never versioned.
graphify-out/
# Local Claude Code settings (copy .claude/settings.example.json here to enable
# the Graphify hooks locally).
.claude/settings.local.json
.claude/settings.json

# Standard-library companion tooling caches.
__pycache__/
*.py[cod]
