# fixture

```rust
// md:Overview
use std::fmt::Debug;
```

```rust
// md:fn labelled
#[inline]
fn labelled<T: Debug>(value: T) -> String {
    format!("{value:?}")
}
```

```rust
// md:impl Tiny
impl Tiny {
    fn one(&self) -> u8 {
        1
    }
}
```

Container marker `// md:impl Container`; members are below.

```rust
    // md:impl Container > fn value
    fn value(&self) -> u8 {
        2
    }
```

Container marker `// md:mod tests`; members are below.

```rust
    // md:mod tests > fn labelled_formats_debug
    #[test]
    fn labelled_formats_debug() {
        assert_eq!(labelled(7), "7");
    }
```
